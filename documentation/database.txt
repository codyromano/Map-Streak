DESCRIPTION OF MySQL SCHEMAS

Table: ctm_places
Columns: 
INT(AUTO_INCREMENT) id
FLOAT latitude 
FLOAT longitude 

Table: ctm_users

VARCHAR(50) name (UNIQUE) 
SMALLINT pin

Table: ctm_access_log
VARCHAR(50) name
TIMESTAMP date 
VARCHAR(100) ip

Note: Originally I was going to store travel ideas in a database, but for the sake of time (and because I don't plan to add many of them), I decided to hardcode the text.

Table: ctm_rewards
VARCHAR(100) (UNIQUE) title
SMALLINT miles_required
VARCHAR(250) image_locked_url
VARCHAR(250) image_unlocked_url


